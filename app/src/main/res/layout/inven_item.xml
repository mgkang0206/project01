<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="fill_parent"
              android:layout_height="wrap_content" android:orientation="horizontal"
              android:paddingTop="10dp"
              android:paddingBottom="10dp">

    <EditText android:hint="Type the item name" android:gravity="center" android:textAlignment="center" android:id="@+id/items" android:layout_width="0dp"
              android:layout_height="match_parent"
              android:textSize="25sp"
              android:layout_weight="1"
              android:inputType="text|textCapCharacters|textCapWords|textCapSentences"
              android:singleLine="true"
              android:editable="false"/>
    <Button
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:text="Stat"
        android:id="@+id/attButton"
        android:layout_gravity="right"
        android:layout_weight=".35"/>

    <Button
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:text="Drop"
        android:id="@+id/dropButton"
        android:layout_gravity="right"
        android:layout_weight=".25"/>


</LinearLayout>
